sudo: required

language: c

services:
  - docker

before_install:
  - docker pull ubuntu:16.04
#  - docker run ubuntu:16.04 /bin/sh -c "apt-get update && apt-get -y install software-properties-common"
#  - docker run ubuntu:16.04 /bin/sh -c "/usr/bin/add-apt-repository -y ppa:elementary-os/os-patches"
  - docker run ubuntu:16.04 /bin/sh -c "apt-get update && apt-get install -y -qq valac libgtk-3-dev libgranite-dev libclutter-gst-3.0-dev libclutter-gtk-1.0-dev"
  - docker run -v .:/tmp/git-dir ubuntu:16.04
  - docker run ubuntu:16.04 /bin/sh -c "cd /tmp/git-dir && cmake -DCMAKE_INSTALL_PREFIX=/usr && make"

script:
  - echo "hello world"
