language: c

install:
  - sudo apt-get update -qq
  - sudo apt-get install -y -qq valac-0.30 libgtk-3.0-dev libgranite-dev libclutter-gst-3.0-dev libclutter-gtk-1.0-dev

script:
  - cmake -DCMAKE_INSTALL_PREFIX=/usr
  - make
